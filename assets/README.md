# Replace this placeholder with your CRT image (the one you uploaded to the chat).
# Name the file screen.png and place it in /assets so the client can load it at /assets/screen.png

# You can upload the image directly to the repo via the GitHub web UI, or use git:
# git add assets/screen.png
# git commit -m "Add CRT image"
# git push
